# Python API Client

Small Python project for interacting with an HTTP API, handling JSON responses, timeouts, and basic formatting.

## Requirements
- Python 3.9+
- `requests`

## Install
```bash
pip install -r requirements.txt
